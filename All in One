#Soalan 1 (Input & Output asas)
#Create a Python program that receives one number from the user and displays the number back to the screen.
print("Soalan 1")
number = input("Enter a number:")
print(f">Your number is {number}")

#Soalan 2 (Operasi aritmetik mudah)
#Create a Python program that receives two numbers from the user and displays the sum of the two numbers.
print("\nSoalan 2")
num2_1 = input("Enter a number:")
num2_2 = input("Enter another number:")
total = float(num2_1) + float(num2_2)
print(f">The total is {total}")

#Soalan 3 (Lebih daripada satu operasi)
#Create a Python program that receives two numbers from the user and displays:
#the sum
#the difference
#the product
print("\nSoalan 3")
num3_1 = input("Enter a number:")
num3_2 = input("Enter another number:")
sum = float(num3_1) + float(num3_2)
diff = float(num3_1) - float(num3_2)
product = float(num3_1) * float(num3_2)
print(f">The sum is {sum}")
print(f">The difference is {diff}")
print(f">The product is {product}")

#Soalan 4 (Penggunaan pembolehubah bermakna)
#Create a Python program that receives the length and width of a rectangle from the user and calculates the area of the rectangle.
print("\nSoalan 4")
print("Let's calculate the area of a rectangle!")
length = input("Enter the length:")
width = input("Enter the width:")
area4 = float(length) * float(width)
print(f">The area is {area4}")

#Soalan 5 (Pengiraan berasaskan formula)
#Create a Python program that receives the radius of a circle and calculates the area of the circle.
#(Use the value of π = 3.142)
print("\nSoalan 5")
print("Let's calculate the area of a circle!")
import math
pi = 3.142
radius = input("Enter the radius:")
area5 = pi * float(radius) * float(radius)
print(f">The area is {area5}")

#Soalan 6 (Pengendalian input jenis teks & nombor)
#Create a Python program that receives:
#the user’s name
#the user’s age
#Then display a message in the format:
#Hello <name>, you are <age> years old.
print("\nSoalan 6")
name = input("Enter your name:")
x = input("Enter your age:")
age = int(x)
print(f">Hello {name}, you are {age} years old")

#Soalan 7 (Penukaran unit)
#Create a Python program that receives a temperature in Celsius and converts it to Fahrenheit.
#Formula:
#F = (C × 9/5) + 32
print("\nSoalan 4")
print("Celcius to Fahrenheit converter")
celcius = input("Enter the temperature in celcius:")
c = float(celcius)
f = c * 9/5 + 32
print(f">The temperature in Fahrenheit is {f}°F")

#Soalan 8 (Pengiraan kewangan mudah)
#Create a Python program that receives:
#the price of an item
#the quantity purchased
#Then calculate and display the total price.
print("\nSoalan 8")
print("Let's calculate the total of your purchase!")
price = input("Enter the price:")
p = float(price)
quantity = input("Enter the quantity:")
q = float(quantity)
totalprice = p * q
print(f">The total price is {totalprice}")

#Soalan 9 (Gabungan pelbagai input & pengiraan)
#Create a Python program that receives:
#the name of a student
#marks for three subjects
#Then calculate and display:
#the total marks
#the average marks
print("\nSoalan 9")
student = input("Enter your name:")
mark1 = float(input("Enter the first mark:"))
mark2 = float(input("Enter the second mark:"))
mark3 = float(input("Enter the third mark:"))
totalmarks = mark1 + mark2 + mark3
avg = totalmarks/3
print(f">Your total marks is {totalmarks}")
print(f">Your average marks is {avg}")

#Soalan 10 (Masalah dunia sebenar – lengkap & berurutan)
#Create a Python program that receives:
#the distance (in km) travelled
#the time (in hours) taken
#Then calculate and display the average speed (km/h) with a clear output message.
print("\nSoalan 10")
distance = float(input("Enter the distance travelled (km):"))
time = float(input("Enter the time taken (hours):"))
avgspd = distance / time
print(f"The average speed is {avgspd}")
